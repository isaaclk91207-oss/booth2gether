{
  "installCommand": "node -e \"const fs=require('fs');let c=fs.readFileSync('pnpm-workspace.yaml','utf8');c=c.replace('packages/*','packages/shared\\n  - packages/frontend');fs.writeFileSync('pnpm-workspace.yaml',c)\" && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @booth2gether/shared build && pnpm --filter @booth2gether/frontend build",
  "outputDirectory": "packages/frontend/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/uploads/:path*",
      "destination": "https://booth2gether-api.onrender.com/uploads/:path*"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "Permissions-Policy", "value": "camera=*, microphone=*" }
      ]
    }
  ]
}
